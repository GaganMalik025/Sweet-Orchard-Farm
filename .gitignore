node_modules
.next
out
build
.DS_Store
*.pem

# env — real values never enter the repo
.env
.env.local
.env*.local

.vercel
next-env.d.ts
*.tsbuildinfo

# raw phone-camera uploads — the cropped, renamed, web-sized versions in
# public/photos are what the site actually serves
temp photos/
